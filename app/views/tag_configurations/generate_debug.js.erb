<%= render :file => "#{src_folder}/modules/HEADER.js" %>

<%= generate_content %>

YUI().use('jshub', function(Y) {

  // Configuration for Causata transport plugin
  jsHub.configure(<%= @tag_configuration.configuration %>);

});
